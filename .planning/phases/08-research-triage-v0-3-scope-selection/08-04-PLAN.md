---
phase: 08-research-triage-v0-3-scope-selection
plan: 04
type: execute
wave: 4
depends_on: [08-03]
files_modified:
  - .planning/phases/08-research-triage-v0-3-scope-selection/08-RECOMMENDATION.md
  - .planning/phases/08-research-triage-v0-3-scope-selection/08-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [TRIAGE-05, TRIAGE-06]
must_haves:
  truths:
    - "Phase 8 recommends one v0.3 package and two alternates."
    - "The exact next GSD command is written in the recommendation and STATE."
    - "ROADMAP and STATE no longer point to stale Phase 7 planning as the next step."
  artifacts:
    - path: ".planning/phases/08-research-triage-v0-3-scope-selection/08-RECOMMENDATION.md"
      provides: "Recommended v0.3 package plus two alternates"
    - path: ".planning/phases/08-research-triage-v0-3-scope-selection/08-SUMMARY.md"
      provides: "Phase 8 closeout traceability"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 8 completion and next v0.3 phase target"
    - path: ".planning/STATE.md"
      provides: "Current position and next command"
  key_links:
    - from: "08-CANDIDATE-MATRIX.md"
      to: "08-RECOMMENDATION.md"
      via: "package selection"
    - from: "08-RECOMMENDATION.md"
      to: ".planning/STATE.md"
      via: "exact next command"
---

<objective>
Close Phase 8 by selecting the next v0.3 scope package, updating project state, and preserving traceability for future agents.

Purpose: give the user one concrete next milestone target without losing the alternates and firewall decisions.
Output: recommendation, closeout summary, ROADMAP update, and STATE update.
</objective>

<read_first>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-research-triage-v0-3-scope-selection/08-VERIFIED-CLAIMS.md
@.planning/phases/08-research-triage-v0-3-scope-selection/08-DISPOSITION-MATRIX.md
@.planning/phases/08-research-triage-v0-3-scope-selection/08-CANDIDATE-MATRIX.md
@.planning/phases/08-research-triage-v0-3-scope-selection/08-SCOPE-FIREWALL.md
@.planning/phases/08-research-triage-v0-3-scope-selection/08-BACKLOG-CLEANUP.md
</read_first>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write v0.3 recommendation package</name>
  <files>.planning/phases/08-research-triage-v0-3-scope-selection/08-RECOMMENDATION.md</files>
  <action>
    Create `08-RECOMMENDATION.md` with sections exactly named `## Recommendation`, `## Alternates`, `## Exclusions`, and `## Exact Next Command`. Select one recommended v0.3 package and two alternates from `08-CANDIDATE-MATRIX.md`. Each package must list included requirement IDs, excluded requirement IDs, why it fits the small hot-path + native `set_cell()` identity, and what would trigger choosing it instead. The exact next command must be concrete, e.g. `/gsd-add-phase ...` followed by `/gsd-plan-phase <new phase number>` if a new phase must be created, or `/gsd-plan-phase 6` only if editor preview is the selected package. Do not select a package that requires Godot terrain-solver delegation, persistent coordinate caches, compatibility shims, version fields, schema markers, or speculative extension points.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$p='.planning/phases/08-research-triage-v0-3-scope-selection/08-RECOMMENDATION.md'; (Test-Path $p) -and (Select-String -Path $p -Pattern '## Recommendation') -and (Select-String -Path $p -Pattern '## Alternates') -and (Select-String -Path $p -Pattern '## Exact Next Command') -and (Select-String -Path $p -Pattern '/gsd-')"</automated>
  </verify>
  <done>`08-RECOMMENDATION.md` names one recommended package, two alternates, exclusions, and the exact next GSD command.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update ROADMAP and STATE with Phase 8 closeout</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
    Update `.planning/ROADMAP.md` to mark Phase 8 planned/executed with 4 plans after all artifacts exist, add the plan list with checked entries, and append or update the next recommended v0.3 phase target using the package chosen in `08-RECOMMENDATION.md`. Preserve Phase 6 as deferred unless the recommendation intentionally selects it. Update `.planning/STATE.md`: current position becomes "Phase 8 complete"; roadmap evolution gains a Phase 8 triage entry; deferred/backlog notes reflect the recommendation and alternates; next step points to the exact command from `08-RECOMMENDATION.md`. Remove stale text that says Phase 7 is the next planning step. Do not mark implementation requirements complete; Phase 8 only completes TRIAGE-01..06.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$r='.planning/ROADMAP.md'; $s='.planning/STATE.md'; (Select-String -Path $r -Pattern 'Phase 8: Research Triage') -and (Select-String -Path $r -Pattern '08-01-PLAN.md|08-02-PLAN.md|08-03-PLAN.md|08-04-PLAN.md') -and (Select-String -Path $s -Pattern 'Phase 8 complete|Research Triage') -and -not (Select-String -Path $s -Pattern 'next planning step remains `/gsd-plan-phase 7`' -Quiet)"</automated>
  </verify>
  <done>ROADMAP and STATE reflect Phase 8 closeout and the exact next GSD command without stale Phase 7 pointers.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write Phase 8 summary and source coverage audit</name>
  <files>.planning/phases/08-research-triage-v0-3-scope-selection/08-SUMMARY.md</files>
  <action>
    Create `08-SUMMARY.md` with: delivered artifact list, TRIAGE-01..06 traceability table, recommended package, two alternates, exact next command, and a multi-source coverage audit covering GOAL, REQ, RESEARCH, and CONTEXT. For CONTEXT, state "No CONTEXT.md exists for Phase 8; authority came from ROADMAP + REQUIREMENTS + RESEARCH + PATTERNS per planning_context." The audit must explicitly say every listed source item is covered or name an unplanned item; do not silently omit anything. Include a "Validation Architecture" section explaining artifact-level validation commands used for Phase 8 because no code/test validation architecture exists in the current research.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$p='.planning/phases/08-research-triage-v0-3-scope-selection/08-SUMMARY.md'; (Test-Path $p) -and (Select-String -Path $p -Pattern 'TRIAGE-01|TRIAGE-02|TRIAGE-03|TRIAGE-04|TRIAGE-05|TRIAGE-06') -and (Select-String -Path $p -Pattern 'Multi-Source Coverage Audit|GOAL|REQ|RESEARCH|CONTEXT') -and (Select-String -Path $p -Pattern 'Validation Architecture')"</automated>
  </verify>
  <done>`08-SUMMARY.md` closes Phase 8 with traceability, coverage audit, validation architecture note, and next command.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Candidate matrix -> recommendation | Ranking becomes a concrete next milestone direction. |
| Recommendation -> ROADMAP/STATE | A design decision becomes project workflow state. |
| Phase artifacts -> future implementation | Future executors may treat package names as authorization to build excluded systems. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Tampering | `08-RECOMMENDATION.md` | mitigate | Require recommendation to cite included/excluded requirement IDs and exact next command. |
| T-08-04-02 | Repudiation | `08-SUMMARY.md` | mitigate | Include TRIAGE-01..06 traceability and source coverage audit. |
| T-08-04-03 | Elevation of Privilege | Next v0.3 package | mitigate | Exclusions section must restate firewall bans and no-compat/no-forward-compat policy. |
| T-08-04-04 | Spoofing | Project state | mitigate | ROADMAP/STATE updates must point to actual Phase 8 artifacts and remove stale Phase 7 next-step text. |
</threat_model>

<verification>
Run all task-level checks, then run `git diff --check`. Optionally run `gsd-sdk query state.planned-phase 08` if available to inspect expected state shape; do not block on that helper if the local GSD version lacks it.
</verification>

<success_criteria>
- TRIAGE-06 is satisfied by `08-RECOMMENDATION.md`.
- TRIAGE-05 is fully reflected in STATE as well as REQUIREMENTS.
- Phase 8 has a closeout summary with traceability and validation architecture.
- The exact next GSD command is present in both `08-RECOMMENDATION.md` and `.planning/STATE.md`.
</success_criteria>

<output>
After completion, create `.planning/phases/08-research-triage-v0-3-scope-selection/08-04-SUMMARY.md` only if the executor's workflow expects per-plan summaries; otherwise `08-SUMMARY.md` is the Phase 8 closeout artifact.
</output>
